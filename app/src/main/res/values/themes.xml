<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.French" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#1a237e</item>
        <item name="colorPrimaryVariant">#000051</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="numbers">
        <item name="android:background">@color/numbers</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="family">
        <item name="android:background">@color/family</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="colors">
        <item name="android:background">@color/colors</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="greetings">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/greet</item>
    </style>
</resources>